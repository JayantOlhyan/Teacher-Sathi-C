import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Sign Up",
  description: "Create your free TeacherSathi account. AI-powered NCERT lesson plans, quizzes, and videos for government school teachers.",
};

export default function SignupLayout({ children }: { children: React.ReactNode }) {
  return children;
}
